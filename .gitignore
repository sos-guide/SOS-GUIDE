# Artefacts de build Rust
target/

# Fabrication de l'image Alpine : produits (.img ~1,5 Go) + scratch (régénérables)
image-alpine/out/
image-alpine/work/

# Base de données / état runtime local
*.redb

# Secrets — JAMAIS dans Git
*.key
*.pem
*.secret
release.key
publish/
.env
.env.*

# Audit local généré + résidus de dev
audit-control/audit.html
audit-control/settings.local.json

# Éditeurs / OS
.idea/
.vscode/
*.swp
.DS_Store
