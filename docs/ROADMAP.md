# ROADMAP.md — feuille de route SOS-GUIDE

> **Fichier vivant.** La liste ci-dessous est le **plan ordonné** de réalisation
> du projet : chaque étape est facultative mais logique, et suit la précédente.
> Claude coche `- [x]` ce qui est réalisé et tient le [Journal](#journal) à jour
> après chaque changement (cf. [CLAUDE.md](../CLAUDE.md)). La page d'audit
> (`just audit`) lit ce fichier : cocher des étapes non faites y génère le prompt
> de la prochaine session.

**Format** : `- [ ]` à faire · `- [x]` fait. Ordre = priorité d'exécution.

## Phase 0 — Socle & outillage

- [x] Structure du dépôt : `docs/` (md), `audit-control/` (build + audit local), `sosguide/` (produit)
- [x] Page d'audit locale `just audit` (progression, erreurs, corrections, génération de prompt)
- [x] Workspace Rust + lints (interdiction `unwrap`/`panic`/`unsafe`), profils dev/release
- [x] Cross-compilation `aarch64-unknown-linux-musl` (binaire statique) + déploiement `just`
- [x] Service systemd (`Restart=always`, journald), webroot optionnel, écoute port 80

## Phase 1 — Domaine & sécurité (fondations)

- [x] `sos-core` : `AlertPacket`, `AlertInbox`, types d'alerte, anti-rejeu (base)
- [x] `sos-security` : trousseau Ed25519, génération + interop clé PEM v2.5
- [x] `sos-storage` : schéma Redb (identité + config), tolérance aux coupures (transactions ACID)
- [x] `sos-storage` : projection **publique** de la config ≠ config complète (`public_config`, corrige la fuite v2.5)
- [x] Persistance de l'identité du nœud dans Redb (fin de l'identité éphémère au reboot)
- [x] Brancher la projection publique du store sur `/data/config.json` (repli fichier si non provisionné)
- [x] `sos-core` : états du cycle de vie complets (`PROVISIONING` → `EMERGENCY`, transition à sens unique)

## Phase 2 — Portail & accès local

- [x] `sos-portal` : routage unique prod/dev, en-têtes de sécurité, projection `config.json` sans `wifiPassword`
- [x] Page d'accueil : splash + guide d'urgence, thème clair/sombre, a11y WCAG, CSP stricte
- [x] i18n 29 langues avec repli français (romanche inclus)
- [x] Synthèse vocale multilingue (TTS Web Speech, hors-Internet)
- [x] Contenus médias dans les fiches : texte, image, vidéo, audio (`safeMedia`)
- [x] Outils d'urgence : RCP, minuterie, flash SOS, lampe, alarme, sifflet, respiration
- [x] Détection de portail captif **multi-OS exhaustive** (Apple/Android/Windows/Firefox/GNOME/KDE/Kindle)
- [x] `sos-portal` : page `/install` — wizard (écrit la config + mot de passe admin dans Redb, bascule EMERGENCY)
- [x] `sos-portal` : page `/admin` — administration locale (auth Basic, édition config/contacts)
- [x] `sos-portal` : état du nœud **public en lecture seule** (`/api/status`) — la page `/audit` autonome a été **fusionnée dans `/admin`** le 2026-06-21 (section « État du nœud » + « Conformité & principes »)
- [x] Couleurs cohérentes sur toutes les pages (fond `var(--bg)` + synchro thème clair/sombre avec l'index)
- [x] **`/install` en formulaire étape par étape** (wizard 4 étapes, validation par étape, lancé une seule fois) → redirige vers `/admin`
- [x] **Carte : configuration** dans `/install` + `/admin` — échelle (aucune/ville/pays/monde), position GPS du nœud (centre), aperçu radar hors-ligne (SVG, sans dépendance)
- [x] **`/admin` : panneau réseau du nœud** (Tor · WiFi · LoRa) — config + statut honnête « non actif (Phase 3/4) »
- [ ] **Dashboard humanitaire collectif** : carte temps réel des **pings GPS d'alerte** (jamais de clients), rayon autour du nœud — nécessite le maillage (Phases 3-4) + GPS dans `AlertPacket`
- [x] **Tuiles OpenStreetMap** réelles : **récupération à l'install + cache** — le nœud télécharge (via `curl` système, TLS hors du binaire pur-Rust) une grille de tuiles autour de son GPS, servie hors-ligne en `/tiles` ; mosaïque centrée + marqueur sur l'accueil, repli SVG si hors-ligne
- [x] `/admin` : rotation fine des secrets (mot de passe admin, WiFi, identité du nœud) + correction des dimensions des champs (`box-sizing`) — _gestion des médias retirée le 2026-06-16 (cf. CHANGELOG)_
- [x] Reprise de l'UX wizard QR / STARTER (langue par défaut du nœud) — **QR WiFi** de l'affiche (`/install` + `/admin`, généré côté nœud en SVG) ; le volet STARTER (langue par défaut) est déjà couvert par `defaultLang` à l'install
- [x] Durcir la validation des entrées (anti-XSS sur champs config, CSRF, rate-limit auth admin)

## Phase 2.5 — Carte de détresse & alerte (prototype web → produit)

> Issu de `prototype-web-sosguide/`. Concept central : l'admin émet une **alerte
> avec cause** ; le portail bascule alors en **page SOS plein écran** (sobre +
> instructions précises) ; les autres nœuds en sont informés via un **tableau
> des nœuds actifs**. Frontend d'abord (données simulées), branché sur le
> maillage réel en Phases 3-4. À traiter **avant la Phase 3**.

- [x] **Carte de détresse interactive** (canvas, sans dépendance ni tuile) : nœud SOS au centre, cercle de portée (~30 m), nœuds distants colorés par réseau (WiFi/LoRa/Tor), zoom carte — _frontend, données simulées (maillage en Phases 3-4)_
- [x] **Ping de détresse** : un appui sur la carte (ou « ping centre ») émet un signal de position **anonyme** (aucune identité de citoyen) visible par tous les nœuds (alimentera `AlertPacket` + GPS en Phases 3-4)
- [x] **Tableau des nœuds actifs** : liste temps réel des nœuds (WiFi/LoRa/Tor) et des pings de détresse en cours (badges réseau) — _frontend, données simulées_
- [x] **Modèle de contenu d'alerte** : la page d'alerte combine des **sources officielles** (gouvernement, météo, catastrophes naturelles, consignes nationales du pays) **mises en cache** pour le hors-ligne + des **consignes locales** ajoutées par l'admin (propres au lieu) — `sos-core::OfficialBulletin`/`OfficialCache`, persistance Redb, affichage sous les consignes sur la page SOS, filtré sur le `countryCode` du nœud (+ bulletins globaux)
- [x] **Ingestion des sources officielles** : **import manuel admin** (repli toujours dispo, hors-ligne) + cache local persistant (`/api/admin/official`, `/api/official`). _Récupération **automatique** sur canal de sortie (Ethernet maintenance / Tor) rattachée à la connectivité — `gateway`/`network` en Phase 3/4 ; alimentera la même persistance. Statut « à la connectivité · Phase 3/4 » affiché honnêtement en `/admin`._
- [x] **Déclenchement & page SOS** : alerte levée par l'admin avec cause → **page SOS plein écran sobre** (cause + consignes précises) ; clôture par `FIN_ALERTE`. _Déclenchement auto par source officielle = rattaché à l'ingestion ci-dessus ; propagation aux autres nœuds en Phases 3-4._
- [x] **Fiabilité & sécurité de l'alerte** (exigence forte) : chemin d'alerte **sans point de défaillance unique** (persistance Redb ACID, survit au reboot ; repli sans base = portail en lecture), validation stricte (cause typée, consignes bornées à 2000 c, `422`/`401`/`503` honnêtes), et **respect de la vie privée** — test `active_alert_carries_no_personal_data` qui **fige** le contrat « cause/consignes/horodatage uniquement, jamais d'identité de citoyen »
- [x] **Transmission aux autorités** : destinataires institutionnels (pompiers, secours…) configurables en `/admin` (nom · canal Ethernet/Tor · adresse · pays), **masqués de la projection publique** (`authorities` dans `SECRET_CONFIG_KEYS` ; test `authorities_hidden_from_public_but_visible_to_admin`). _Relais effectif **quand un canal de sortie existe** — statut honnête « aucun canal · Phase 3/4 » + « N destinataire(s) en attente de relais » si alerte active._
- [x] **i18n de la page SOS** : cause + consignes génériques affichées dans la langue de l'utilisateur (helper partagé `lib/sos-i18n.js`, repli français). _7 langues vérifiées (fr, en, de, it, es, pt, nl) ; les consignes **locales** saisies par l'admin ne sont pas traduites (texte tel quel)._
- [ ] **Traductions vérifiées des langues restantes** (22, dont le **romanche** — exigence PCi-CH) : relecture **humaine** du texte de sécurité avant ajout dans `lib/sos-i18n.js` (jamais de traduction automatique pour des consignes vitales)
- [x] **`index.html` one-page** : corps `#mainContent` dense et défilant (accueil + carte + nœuds + réseau + Morse + sections de survie + LoRa). _Le splash d'entrée est **conservé** (accès immédiat aux numéros d'urgence avant tout) — décision produit ; retrait éventuel rattaché à l'arbitrage « Style & UX »._
- [x] **Navigation one-page** : barre supérieure (logo · langue · thème · menu) + tiroir (`#drawerNav`, liens data-driven) + défilement par section
- [x] **Statuts réseau temps réel** (LED LoRa/Tor/WiFi) — section « État du réseau » avec LED **honnêtes rouge « non actif · Phase 3/4 »** tant que `sos-network`/`sos-radio`/`sos-gateway` ne sont pas livrés
- [x] **Bibliothèque de survie catégorisée** : sections pilotées par les JSON de langue (principes, outils de survie, urgences médicales, situations collectives, associations & droits) reliées aux fiches existantes (la taxonomie reste extensible côté `data/*.json`)
- [x] **Outil Morse** (tableau international + émission **lumineuse plein écran** & **sonore** Web Audio, hors-ligne) en complément des outils d'urgence existants
- [x] **Langue à l'installation** : 1ère étape du wizard `/install` = **choix de la langue** (grille des 29 langues), persistée en `defaultLang`
- [x] **Pays déduit de la langue à l'install** : la langue propose/filtre le **pays** (1er = défaut) ; l'interface s'**adapte au pays** — préremplissage des numéros d'urgence (FR 15/17/18, CH 144/117/118, repli 112), `countryCode` persisté. _Sources officielles par pays = item ingestion ci-dessus._
- [ ] **Toutes les pages portail multilingues** : `/install`, `/admin` et page SOS, pas seulement l'accueil (système i18n unifié + repli français)

## Phase 3 — Réseau local

> **Décision** : tout le code `sos-network` est écrit et testé, **gaté par
> `SOS_NET_MODE`** (`off` par défaut). Le **go-live réel** (hostapd/iptables sur
> `wlan0`) est **différé** : sur ce Pi `wlan0` est la ligne SSH → l'activer =
> lockout. Levée du garde-fou quand un accès alternatif existera (eth0/dongle) +
> `hostapd`/`iptables` installés.

- [x] `sos-network` : AP WiFi via `hostapd`/nl80211 — génération de conf (open vs WPA2) testée + applier gaté `live` _(go-live différé)_
- [x] `sos-network` : DNS local fait main (toute requête `A` → IP du nœud, portail captif ; `AAAA` vide, IPv6 off) — codec testé
- [x] `sos-network` : DHCP local fait main **sans baux persistés** (pool 10.0.0.10–250, en mémoire), IPv6 désactivé — codec + allocation testés
- [x] `sos-network` : isolation `FORWARD DROP` + hashlimit par IP (~30 req/s) + DoT/853 bloqué — générateur d'argv `iptables` testé, applier gaté `live`
- [x] `sos-network` : mode WiFi **on/off commutable** — `SOS_NET_MODE` (`off`/`simulate`/`live`), recettes `just net-sim` + `just wifi-on`/`wifi-off` _(live différé, garde-fou `SOSGUIDE_GO_LIVE`)_
- [x] Résolution locale `sos.guide` → IP du nœud (DNS du nœud répond toute requête `A`)
- [x] Transition à chaud `PROVISIONING` → `EMERGENCY` (et AP protégé↔ouvert selon l'alerte) sans reboot — canal `watch<RuntimeSignal>` portail→réseau, décision `plan_for` testée

## Phase 4 — Radio & longue distance

- [x] `sos-radio` : transport + relais mesh, **canal réservé exclusivement aux alertes** — codec trame JSON v2.5 (dans `sos-core`), `relay::evaluate` pur (vérif signature → dédup/anti-rejeu → relais `hop++` sous plafond), orchestrateur gaté `SOS_RADIO_MODE` (`off`/`simulate`/`live`) ; trames non signées/non fiables **jetées** _(Reticulum non retenu : transport ligne par ligne ; activation différée)_
- [ ] `sos-radio` : pilotes SX1276 (SPI) et Meshtastic T-Beam (USB) — **différé** (aucun matériel LoRa branché ; `device::open` échoue proprement, mode `live` = no-op journalisé)
- [x] `sos-portal` ↔ `sos-radio` : pousser la trame d'alerte publiée vers le mesh — `NodeState.radio_tx` (canal `mpsc`), `POST /api/alerts` diffuse la trame signée ; `inbox`+`keyring` partagés (`Arc`) entre portail et radio
- [x] **Registre des nœuds de confiance chargé au démarrage** (`SOS_TRUSTED_NODES`, `trusted_nodes.json` v2.5) — débloque le maillage multi-nœuds (sans lui, seul le nœud lui-même est de confiance)
- [x] **Gestion admin du registre de confiance** : persistance Redb (table `trusted`), API `GET/POST/DELETE /api/admin/trusted` (auth) avec **rechargement à chaud** du trousseau partagé ; boot priorise Redb puis importe le fichier v2.5 ; **rotation de la clé du nœud préserve le registre** ; **panneau `/admin`** : liste des pairs + remplacement/purge du registre
- [x] `sos-gateway` : service caché Tor v3, surface `.onion` **restreinte au manifeste** — manifeste public (identité + phase + état d'alerte, **aucun secret/portail/admin/config**) servi en loopback dédié, `torrc` v3 généré, orchestrateur gaté `SOS_GW_MODE` (`off`/`simulate`/`live`) _(lancement du démon `tor` différé : absent du Pi)_ ; le manifeste reflète l'état runtime via le canal `watch` partagé
- [x] `firmware/esp32-lora` : **initialisé** — cible **ESP32-C3 (RISC-V)**, projet `no_std` (workspace séparé), init `esp-hal` 1.x + LED de vie + `build_frame()` au format trame v2.5 (`heapless`, interop `sos-core`). **Vérifié par cross-compilation** (`cargo build --release`, ELF 126 Ko, sans matériel). _Embassy différé (handshake de versions `esp-hal-embassy`/`esp-hal` à figer au flash) ; pilote SX127x (SPI) + boucle mesh avec le matériel._

## Phase 5 — Supervision & mises à jour

- [x] `apps/sos-guide` : supervision interne typée (remplace l'orchestration shell), zéro `sudo` depuis le web — sous-systèmes lancés en tâches `tokio` typées (réseau/radio/passerelle/portail), aucune exécution shell depuis un handler HTTP ; les seules élévations passent par `just`+sudo (hors web)
- [x] `sos-cli` : sonde santé (température, RAM, charge, disque) + watchdog `bcm2835_wdt` — binaire `sos-cli health` (vitaux JSON depuis `/sys`+`/proc`+`df`, parsing pur testé ; recette `just vitals`) et `sos-cli watchdog` (caresse `/dev/watchdog` tant qu'une **sonde applicative** TCP juge le démon sain, désarmement propre au `Drop`) _(unité systemd du watchdog à brancher au déploiement)_ ; vitaux exposés à l'admin (`GET /api/admin/vitals` + panneau « État du nœud » de `/admin` : température/RAM/disque/charge en direct)
- [x] Supervision des services systemd + collecte journald (erreurs → `ERRORS.md`) — `just journal` capture les avertissements/erreurs runtime (`journalctl -p warning`) dans l'inbox d'`ERRORS.md`, curés ensuite par `just errors`
- [x] MAJ binaire **atomique avec rollback** (sauvegarde) — `just update` : backup `.prev`, installe par **rename atomique**, redémarre, vérifie `/api/status`, **restaure automatiquement** si la santé échoue
- [x] MAJ OS maîtrisée (`apt`) déclenchée hors-bande (Ethernet/Tor) — `just os-update` (update + full-upgrade + autoremove, sudo TTY, signale un redémarrage requis)
- [x] Manifeste de version **signé** + vérification — `sos-core::VersionManifest` (version + SHA-256 + date, charge canonique), signature Ed25519 détachée (`sos-security::{sign,verify}_detached`), `sos-cli sign-update`/`verify-update` (empreinte + signature), recette `just verify-release` _(MAJ OS apt hors-bande : reste)_

## Phase 6 — Image de production propre

- [x] Durcissement systemd (sandbox compatible avec les besoins réseau) — `ProtectSystem=strict` + `ReadWritePaths=/var/lib/sos-guide`, `PrivateTmp`, `ProtectKernel*`, `ProtectControlGroups`, `RestrictAddressFamilies` (IP/UNIX/NETLINK), `RestrictSUIDSGID`/`RestrictRealtime`/`LockPersonality` _(mode `live` : ajouter /etc/hostapd + `ProtectKernelTunables=false`)_
- [x] Logs volatils en tmpfs, aucune donnée personnelle (conformité nLPD/RS 235.1 + RGPD) — drop-in `journald-sosguide.conf` (`Storage=volatile`, `RuntimeMaxUse=32M`) installé par `just install`
- [x] Rootfs en lecture seule en remplacement de l'intégrité `chattr`/SHA v2.5 — **assuré par l'image PRODUIT Alpine diskless** (OS-en-RAM + SOSDATA montée ro en permanence, cf. section « Durcissement appliance Alpine »). _L'ancien outillage `overlayroot` pour Raspberry Pi OS Lite (`sosguide/image/`) a été retiré : superSédé par l'approche Alpine._
- [x] `PRIVACY.md` affichée sur le portail (mention courte « Aucune donnée personnelle collectée ») — page `/privacy.html` (rendu markdown léger, thémée) + mention et lien dans le pied du tiroir de l'accueil
- [x] Build de l'image `.img` reproductible et propre — **livré via `image-alpine/`** (Alpine Linux diskless, `assemble.sh` rootless reproductible ; cf. section « Durcissement appliance Alpine »). _L'ancien chemin Raspberry Pi OS Lite (`sosguide/image/` : harden.sh/readonly.sh/README) a été retiré. Production du `.img` final = matériel (Pi/SD de build)._

## Durcissement appliance Alpine — SOSDATA en lecture seule

> Image `image-alpine/` (Pi 4, *diskless*, OS-en-RAM). SOSDATA est montée **ro**
> en permanence ; seules des fenêtres rw ponctuelles (admin) l'ouvrent, toujours
> re-verrouillées et surveillées par un garde-fou.

- [x] Persistance scindée : base Redb **de travail** en tmpfs/RAM (`/run`), **instantané durable** sur SOSDATA — `Store::open_durable` + `persist_durable()`, restauré au boot (config/identité/secrets sans reconfiguration)
- [x] Instantané Redb via helper privilégié `sos-commit-db` (remonte rw → copie atomique → remonte ro ; `flock` + `trap`)
- [x] Fenêtre rw des tuiles : garde RAII `RwWindow` (`SOS_RW_CMD` open/close), sérialisée (`tiles_lock`), autour de `/api/admin/map` et de la purge factory-reset — helper `sos-rw`
- [x] **Garde-fou ro périodique (`crond`)** : `sos-ro-guard` re-verrouille SOSDATA en ro si laissée rw (orpheline après un SIGKILL) sans fenêtre légitime — marqueur `/run/sos-rw.active` + TTL ; services `syslog` (provide logger) + `crond` activés
- [x] Image bootable reproductible (apkovl, binaires statiques aarch64-musl, build rootless conteneur `alpine:3.21`)

## Cockpit (optionnel, gelé — hors img produit)

> Outillage de dev dans `audit-control/`. Gelé en l'état tant que le produit
> avance ; enrichi au fil de l'eau, jamais avant le produit.

- [ ] (cockpit) agent-audit dédié (gère l'audit de façon autonome)
- [ ] (cockpit) monitoring temps réel : warnings cargo, journald, CPU/mém/process/kernel
- [ ] (cockpit) liste exhaustive de toutes les erreurs (build + runtime Pi)

## Style & UX (prototype web → produit) — priorité basse

> Présentation uniquement. Volontairement **en fin d'audit** : à traiter une
> fois les fonctionnalités ci-dessus en place. Reprend le design du
> `prototype-web-sosguide/` (barres haut/bas, tiroir, thème, accessibilité).

- [x] **Gabarit CSS commun** : tokens de thème (`lib/sos-theme.css`) + logique de thème (`lib/sos-theme.js`) + bouton `.theme-btn` **partagés** par toutes les pages (`/`, `/install`, `/admin`, `/privacy`) ; aucune page ne redéfinit `:root` _(barre de nav et pied restent propres à chaque contexte : accueil citoyen vs wizard vs admin)_
- [x] **Thème clair/sombre** unifié et persistant — un seul `lib/sos-theme.js` (clé localStorage commune `sos_guide_theme`, applique avant peinture, câble le toggle de nav `#themeToggle` ou injecte un bouton flottant), cohérent sur toutes les pages
- [x] **Accessibilité — zoom de texte** (A− / A+ : petit/normal/grand/très grand = 87,5/100/112,5/125 % du rem racine), cible WCAG 1.4.4, **persistant** (clé `sos_guide_textsize`), contrôle injecté sur toutes les pages par `lib/sos-theme.js`
- [x] **Design barres haut/bas + tiroir** repris du prototype — barre du haut + tiroir existants conservés ; ajout d'une **barre du bas** d'accès rapide (Carte · Urgences · Guide · Outils · Menu) à grandes zones tactiles (≥60 px), `safe-area-inset-bottom` (viewport-fit=cover), thémée, masquée au splash et sous les overlays SOS ; le tiroir s'ouvre depuis « Menu »
- [x] **Splash / mode survie** : splash conservé (arbitrage Phase 2.5) avec numéros d'urgence visibles d'emblée ; **mode survie** = une fois « Entré », les visites suivantes vont **directement au contenu** (accès rapide guide/carte, `sos_guide_entered`), le splash restant joignable via le logo

## Journal

<!-- Claude ajoute UNE ligne datée en tête après chaque changement notable. -->

- `2026-07-03` 🕸️ **Maillage LoRa du paiement (créneau 3) : `sos-pay` ↔ `sos-radio`.** Le canal LoRa transporte fragments de tx **en plus** des alertes, **alertes-first** (sélection biaisée : réception + alertes avant paiement). Émission : `submit_pay` fragmente la tx et pousse vers la radio (`NodeState.pay_tx`), statut → `relayed`. Réception : trame non-alerte → `Reassembler` → à tx complète, mise en file (`Relay::accept_raw`). Relais **partagé** portail↔radio. **Vérifié en natif** (POST → 3 fragments `sent=3` → `relayed`) + 2 tests unitaires (réassemblage entrant, émission sortante). 164 tests, clippy `-D warnings` propre. **Reste (matériel) : pilote LoRa `live` SX1276/Meshtastic + diffusion `live` `curl`** — à écrire avec le module branché.
- `2026-07-03` 🔌 **`sos-pay` câblé : app + endpoints portail `/api/pay`.** L'app monte le relais selon `SOS_PAY_MODE` (**off par défaut** ⇒ désactivé, zéro impact vital ; `SOS_PAY_BROADCAST_API` optionnel) et l'injecte dans `NodeState.pay`. Portail : **`POST /api/pay`** (dépose une tx signée hex → file ; `202`/`200` doublon/`422`/`503`, garde CSRF) + **`GET /api/pay`** (état public `enabled`/`queued`/`pending` + `{id,size,status}`, sans exposer les tx brutes). **Vérifié en natif** : off → `{"enabled":false}` + `POST` 503 ; simulate → dépôt 202, doublon 200, hex invalide 422, `GET` liste la file. Workspace : clippy `-D warnings` propre, 162 tests. Reste : canal mesh `sos-radio` + diffusion `live` (matériel LoRa).
- `2026-07-03` ₿ **Crate `sos-pay` — « Bitcoin tx over LoRa » (mode urgence), cœur gaté.** Premier créneau du relais de paiement décidé dans `ASK.md` : nouveau crate **isolé, `SOS_PAY_MODE=off` par défaut** (zéro impact portail vital). La borne = **transporteur** de tx signées (ni clé ni fonds). Modules purs + testés : `tx` (validation format/taille + hex sans dépendance), `frame` (fragmentation ~120 o + réassemblage désordonné), `queue` (file bornée anti-doublon), `policy` (**alertes-first** + rate-limit duty cycle), `broadcast` (argv `curl` → API publique). `Relay` compose le tout ; test **pipeline bout-en-bout** (accept → fragment → réassemble → diffuse). **17 tests, clippy `-D warnings` propre, compile aarch64-musl.** Live/matériel LoRa + câblage app/portail/mesh **différés**.
- `2026-07-03` 🚑 **Borne réparée + validée sur Pi réel.** Le portail était injoignable au boot : la route `/api/admin/groups/:id` (syntaxe capture axum 0.7) faisait **paniquer `Router::route` sous axum 0.8** → nœud mort au démarrage (AP/DHCP OK, rien sur :80), invisible aux tests (aucun ne construisait le routeur). Corrigé `:id`→`{id}` + test `router_builds_without_panicking`. Diagnostiqué en lançant l'appli en natif (SIGABRT + backtrace). Image aarch64-musl reconstruite, **flashée et confirmée fonctionnelle sur Raspberry Pi** (portail servi sur Wi-Fi ouvert `SOS-GUIDE`). Au passage : point de montage `/var/lib/sosguide` créé (persistance SOSDATA), diagnostic de boot `SOSGUIDE_DIAG.txt` ajouté. Décision v2 « Bitcoin tx over LoRa » (Monero abandonné) consignée dans `ASK.md`.
- `2026-07-02` 🧹 **Passe de cohérence « prêt-client » : retrait du code mort + désencombrement.** (1) **Machinerie clé WiFi entièrement retirée** — l'assistant `/install` distribuait encore un `wifiPassword` + QR `T:WPA` tout en affichant « WiFi ouvert » (contradiction sur le chemin vital). Supprimés : `generate_wifi_key`/`WIFI_KEY_LEN` (security), enum `Security` + branche WPA de `hostapd_conf` + champ `wifi_key` (network), génération/rotation `wifiPassword` + `/api/admin/wifi-qr` clé→ouvert + `wifi_join_string` `WPA`→`nopass` (portal), affichage clé dans `install.html`/`admin.html`. Purge défensive `wifiPassword` conservée (config héritée v2.5). i18n `fr` corrigé (`done_net`/`done_note`, `net_*`) ; 28 langues = relecture humaine (standby). (2) **`sosguide/image/` (RPi OS Lite) supprimé** — superSédé par l'image produit Alpine (`image-alpine/`) ; recettes `just image-harden`/`image-readonly` retirées ; Phase 6 (rootfs RO + `.img`) marquée assurée par Alpine. (3) **`webpage/` sorti du dépôt** (→ `sosguide.fr/webpage/`, 87 Mo, `.gitignore`). (4) **`settings.local.json`** résidu obsolète purgé. **Vérifié** : `cargo clippy --workspace` **0 warning**, `cargo test --workspace` **144 tests, 0 échec** ; `just --list` parse. Aucune référence `wifiPassword`/clé WiFi restante dans `web/`.
- `2026-06-28` 📡 **Mode AP opérationnel câblé dans l'image Alpine + `.img` reconstruite (modèle décidé)**. Méthode tranchée avec l'humain (`ASK.md`) : **hostapd + dnsmasq** (recette Pi éprouvée), appli en `SOS_NET_MODE=off` (sert le portail). Nouvel init **`sos-netmode`** (remplace le `sos-wifi` client) : installe les paquets AP hors-ligne, `modprobe brcmfmac`, monte **wlan0 = AP OUVERT** 10.0.0.1/24 (hostapd : réseau ouvert, `country_code` éditable, `ap_isolate`), **dnsmasq** (DHCP + DNS portail captif `address=/#/10.0.0.1`, `leasefile-ro`), **`ip_forward=0` + `FORWARD DROP`** (aucun surf) ; **eth0 = uplink optionnel** → si lien : `udhcpc` + **avahi** `sosguide.local` (sans D-Bus), sinon autonomie totale ; écrit `BOOT_OK.txt`/`DIAG.txt` sur la FAT. **Paquets** : `boot-extra/aarch64` remplacé (hostapd/dnsmasq/iptables/avahi + deps, hors base ; wpa_supplicant client retiré). **Conf FAT** `ap.conf` (SSID/COUNTRY/CHANNEL = SOS-GUIDE/FR/6, éditable) ; `wifi.conf` client **supprimé**. **Binaires d'usine musl recompilés** (sos-guide 2,2 Mo + sos-cli 943 Ko, statiques) et posés dans l'apkovl ; `/etc/hostname=sosguide`, conf avahi. **Sécurité/vie privée** : **toutes les traces du réseau perso du dev purgées** (`NETGEAR`/PSK retirés de `boot/`, scratch `work/` nettoyé via docker root, absents de git) — `grep` du `.img` final = **zéro trace**. **Image reconstruite** `out/sosguide-3.21.7.img` (1,5 Go → **71,9 Mo `.xz`**) + checksums, **vérifiée** (mtools : FAT a `ap.conf`+`/extra` AP, apkovl a `sos-netmode`+binaires, pas de `sos-wifi`/`wifi.conf`). `audit.html` régénéré (91 %). **NON testable ici (besoin du Pi)** : boot réel, diffusion de l'AP, `apk add` hors-ligne des 20 paquets, portail captif sur téléphone → à flasher + valider (le `DIAG.txt` aide). OTA reste inerte (pas de clé de publication encore).

- `2026-06-28` 🧭 **Cap & vérité : `CLAUDE.md` réaligné + `ASK.md` créé + modèle réseau tranché** (demande user : « prépare le terrain », `CLAUDE.md` priorité ABSOLUE, le plus réel/fidèle à l'enjeu vital). (1) **`CLAUDE.md`** : ajout d'un encadré **enjeu vie-ou-mort / intérêt public** en tête ; QQOQCP et section « Plateformes » réécrites pour refléter la **réalité produit = image Alpine diskless** (musl statique OS-en-RAM, SOSDATA ro, OTA) — correction de la contradiction « plus de cross-compile musl » (vrai pour le dev natif glibc, faux pour l'image produit) ; colonne « État » des crates remise à jour (storage/portal/security/cli/network/radio/gateway = mûrs/code-complet, plus « partiel/squelette ») ; `sos-cli update` + OTA intégrés ; priorité « fiabilité » re-justifiée par l'enjeu vital. (2) **Modèle d'accès réseau GRAVÉ (la meilleure méthode)** : `wlan0` = **AP toujours actif** + `eth0` = **uplink optionnel** → l'**install est opérationnelle sans Ethernet par construction** (l'admin se connecte à l'AP), l'uplink n'ajoute que des extras (OSM/OTA/Tor, `sosguide.local` mDNS) et pas de tuiles OSM sans uplink (dégradation gracieuse, repli SVG) ; radio unique → uplink filaire, zéro conflit ; AP-always > AP-fallback car *toujours joignable*. (3) **`ASK.md`** créé (dialogue humain↔agent : décisions + questions ouvertes des deux côtés + idées rangées dont le chat écarté). **Aucun code touché** (préparation). **Prochain pas** : câbler ce modèle dans l'image Alpine (paquets `hostapd`+`avahi` aarch64, init de sélection de mode, bascule `live`), test sur Pi (sans risque de lockout : appliance Alpine ≠ Pi de dev). _En attente : confirmations user dans `ASK.md` (AP veille ouvert/protégé, nom mDNS, cible LoRa, traductions, certification)._

- `2026-06-27` 🚀 **MAJ OTA de la flotte (« pull » signé, image Alpine diskless)** — réponse à « mettre à jour tous les nœuds sur Ethernet ». Modèle **pull** (chaque borne tire un manifeste signé) + **reboot** + **cron auto**, choisis avec l'utilisateur. Contrainte diskless résolue par un **slot A/B sur la FAT** : binaire d'**usine** conservé dans l'apkovl (anti-bricage), **slot OTA** surchargé au boot s'il vérifie et est ≥ usine. **Rust** : `sos-cli update` (`crates/cli/src/update.rs`) — parse `update.conf`, `curl` système (jamais de HTTPS Rust en musl), réutilise `verify_detached`+`matches_binary`, **anti-downgrade** (`version_gt`), délègue l'install au helper privilégié ; 4 tests unitaires (version/conf/url) verts, clippy+fmt OK. **Shell** (overlay) : `sos-apply-update` (slot A/B + marqueur pending + reboot), `sos-boot-select` (start_pre : rollback si `update.failed`, sinon installe le slot vérifié ≥ usine), `sos-update-confirm` (start_post : santé `/api/status` → confirme ou `update.failed`+reboot), `sos-update` (cron `flock -n`). **Image** : `etc/sosguide/{release.pub,manifest.json}` épinglés, `boot/update.conf` (FAT, **ENABLED=0** par défaut → OTA inerte), crontab `*/30`, garde apkovl si `sos-cli` absent. **Opérateur** : `just release-keygen` (Ed25519, privée locale via `.gitignore`) + `just publish-release <ver>` (build musl + sign-update → `publish/` + rafraîchit l'image). **Vérifié** : `cargo test -p sos-cli` (13 OK), clippy/fmt propres, `sh -n` des 4 helpers + `assemble.sh`, **busybox réel** (Docker alpine:3.21) pour `awk vge`/`sed` version/`flock -n`. _Non exécuté ici (besoin toolchain musl + clés + Pi) : build/clé/publish réels, image reconstruite, MAJ de bout en bout sur Pi. Sécurité : signature obligatoire, anti-downgrade, une seule sortie vers URL épinglée, zéro sudo web, rollback santé._
- `2026-06-27` 🐛 **Associations & numéros d'aide : suivent le pays, plus la langue** (même principe que les numéros d'urgence, cf. règle de sécurité). Elles venaient de `fr.json` → **toujours françaises** quel que soit le pays. Désormais **configurables** : éditeur dynamique **nom + numéro (1 à N)** à **`/install`** (nouvelle étape « Associations », après Contacts) **et `/admin`** (section dédiée, sauvegarde propre, calquée sur l'éditeur « autorités »). Persistées dans `config.associations` (non secret → présent dans la projection publique). Le portail (`index.html`) lit `configData.associations` et l'affiche **à la place** de `langData.associations` ; **repli** sur la langue **seulement si vide** (rétro-compat FR). **Zéro changement Rust** : la config est un JSON libre — `install_node` la persiste telle quelle, `admin_set_config` la **fusionne**, `validate_config_value` accepte les tableaux d'objets de chaînes, la projection publique ne retire que les clés secrètes. Fichiers : `web/{index,install,admin}.html` + clés i18n (`install-i18n.json` `assos_*`, `admin-i18n.json` `assoc_*`/`js_assoc_*`, repli FR), **propagés à l'overlay image**. **Vérifié** : JSON valide, JS des 3 pages `node --check` OK, flux de bout en bout relu (install → store → projection → portail ; admin merge). _Rendu/usage réels à valider sur Pi ; relecture humaine des libellés i18n due._
- `2026-06-27` 🎨 **Cohérence visuelle inter-pages (accueil ⇄ install) + Morse outil** : (1) **barre de menu de `/install` rendue identique à l'accueil** — logo ⛑️ SOS-GUIDE, hamburger + tiroir, `A−/A+`, 🌐 langue, 🌙 thème ; les règles de cette barre (+ tiroir + modale de langue + carte `.app-card`) sont **extraites dans `lib/sos-theme.css`** = source unique partagée (l'accueil conserve sa copie inline historique, identique). `/install` perd son `.topbar` maison ; le `<body>` reçoit un `padding-top` sous le `#nav` fixe. **Collision d'`id=nav` corrigée** : la rangée de boutons du wizard devient `#navButtons` (refs JS mises à jour) ; la règle `button{flex:1}` est **scopée `.nav button`** pour ne pas déformer les `.nav-btn`/`.hamburger` du menu. (2) **Carte de présentation du projet en 1ʳᵉ étape** de `/install` (« Le projet » : sans Internet, 29 langues, aucune donnée perso ; clés i18n `intro_*` ajoutées au dict `fr`, repli FR pour les 28 autres). (3) **Morse promu en outil** : retrait de la section figée `#morseSection` de l'accueil → **carte carrée** dans la grille d'outils ouvrant une **modale** (`#morseModal`, IIFE Morse inchangée car mêmes IDs) ; ajouté pour **toutes les langues** (repli FR, libellés `tools.morse` dans `fr.json`). Source `sosguide/web/{index,install}.html` + `lib/sos-theme.css` + `data/{fr,install-i18n}.json`, **propagée à l'overlay image** (5 fichiers, overlay re-synchronisé identique). **Vérifié** : JSON valide, unicité de tous les IDs Morse + `#nav`/`#navButtons`, aucun reliquat `morseSection`/`.topbar`. _Image `.img` à reconstruire + **rendu visuel à valider sur Pi** (réinsertion SD ou reflash) ; relecture humaine des traductions `intro_*` toujours due._
- `2026-06-27` 🎨 **Retouches d'affichage portail (accueil + install)** : (1) zoom texte `A−/100%/A+` + thème **en haut** de `/install` (parité accueil) ; (2) **carte OSM masquée** sur l'accueil (`#carteSection`) et **facultative** à l'install (case `#mapEnabled` → `mapScope=none`, pas de téléchargement de tuiles si décochée) ; (3) **barre du bas** à **hauteur fixe** (`62px + safe-area`, ne grandit plus au scroll) + onglets dans l'**ordre de la page** (Outils·Urgences·Guide·Aides, onglet Carte retiré) ; (4) **outils en cartes carrées** (`aspect-ratio:1`, desc masquée) ; (5) **Urgences médicales / Situations collectives / Associations** en **tableaux** cliquables (`buildInfoTable`, ligne→fiche, `tel:` pour les assos). Source `sosguide/web/{index,install}.html`, propagée à l'overlay image, image reconstruite + `.img.xz`/checksums. **Vérifié** : syntaxe JS des 5 blocs (`new Function`), équilibre `<div>` de l'étape Position, overlay identique. _Libellés du toggle/onglets en FR (i18n à compléter) ; rendu visuel à valider sur Pi._
- `2026-06-27` 🐛 **Install image Alpine corrigée (« administration non configurée ») — `flock -w` busybox**. Après install, définir le mot de passe admin échouait puis `/admin` répondait « administration non configurée ». **Cause** : `sos-commit-db` (commit du snapshot durable sur SOSDATA) utilisait `flock -w 5 9`, option **`-w` absente du `flock` busybox** → helper en erreur à chaque écriture → `persist_durable` `Err` → `save_config`/`save_admin_password` `Err` → wizard `/install` KO, mot de passe jamais persisté. **Correctif** : attente bornée ~5 s émulée via `flock -n` en boucle (portable busybox). **Vérifié** en conteneur Alpine (`ash -n` + exécution) : commit OK lock libre, marqueur retiré au `trap`, attente correcte sous contention 3 s. Image reconstruite (inclut aussi `wifi.conf`), `.img.xz` + checksums régénérés. Seul `sos-commit-db` touché (`sos-rw`/`sos-ro-guard` n'utilisent pas `flock`). _À confirmer : install complète sur Pi réel._
- `2026-06-27` 📶 **WiFi client configurable sans recompiler — `SOSBOOT/wifi.conf`**. Le service `sos-wifi` lit `/media/mmcblk0p1/wifi.conf` (racine FAT, éditable depuis n'importe quel PC) — format `SSID`/`PSK`/`COUNTRY`, tolère CRLF Windows, SSID à espaces, réseau ouvert (`PSK` vide → `key_mgmt=NONE`), `SSID` vide ⇒ mode client ignoré sans erreur — et génère la conf `wpa_supplicant` à chaud (ancien fichier figé retiré). `assemble.sh` dépose un `wifi.conf` documenté par défaut. **Vérifié** : parsing (CRLF + `&` + pays surchargé), `ash -n`, présence dans l'image (FAT + apkovl). _Confort de TEST ; en exploitation finale la borne sera en **AP seul**, Ethernet/Tor/LoRa gardant leur fonction propre._
- `2026-06-27` 🐛 **WiFi image Alpine corrigé — paquets aarch64 (était x86_64)**. Analyse du `DIAG.txt` du dernier boot Pi : tout chargeait (Alpine diskless, apkovl, driver `brcmfmac` + firmware BCM4345/6, `wlan0` présente) **sauf** l'install des paquets WiFi — `apk add --no-network` échouait (`conflicts: musl … so:libc.musl-x86_64.so.1`), donc `wpa_supplicant`/`iw` absents → pas d'association → `wlan0` `NO-CARRIER` → aucun bail DHCP. **Cause** : les `.apk` de `image-alpine/boot-extra/aarch64/` étaient en fait **x86_64** (vérifié via `.PKGINFO`), téléchargés sur l'hôte sans `--arch`. **Correctif** : re-fetch de la clôture en **aarch64** (`docker run alpine:3.21` → `apk fetch --arch aarch64 -R wpa_supplicant wpa_supplicant-openrc iw`, mêmes noms/versions) + **retrait de `musl`** (déjà dans la base diskless, même `r11` = source du conflit `world[]`). **Vérifié** : 8 paquets `arch=aarch64`/`noarch` confirmés dans l'arbre source, dans `/extra` de l'image `.img` reconstruite (lecture FAT via `mtools`) **et** sur la carte SD montée (corrigée en place pour retest sans reflash). `.img.xz` + checksums régénérés. **✅ Confirmé sur Pi réel (2026-06-27)** : boot OK, WiFi associé, portail accessible sur `http://192.168.1.133/`.
- `2026-06-26` 🔒 **Garde-fou ro périodique (`crond`) + vignette d'audit — « SOSDATA ro 100 % » finalisé** : dernier maillon du modèle SOSDATA en lecture seule (image Alpine *diskless*). `sos-ro-guard` (cron chaque minute) re-verrouille SOSDATA en `ro` si trouvée `rw` sans fenêtre d'écriture légitime (marqueur `/run/sos-rw.active` absent/périmé, TTL 300 s) — couvre le `SIGKILL` du nœud pendant un instantané Redb ou une écriture de tuiles. `sos-rw`/`sos-commit-db` posent le marqueur le temps de leur fenêtre. Services `syslog`+`crond` activés (apkovl), crontab `/etc/crontabs/root`. **Vérifié** : détection rw/ro via `/proc/mounts` (rw→agir, ro/autre montage→skip), logique TTL, `sh -n` des 3 helpers ; apkovl régénéré (helpers + crontab + symlinks présents), image recompressée 66 Mo. **Page d'audit améliorée** : `audit-gen.py` rend une vignette « Durcissement appliance » (badges ✓/⚠ + sceau `🔒 SOSDATA ro · 5/5`) depuis une section dédiée de ROADMAP (exclue du décompte d'étapes), libellés nettoyés du markdown. _Exposition rw d'une fenêtre orpheline bornée ≤ 60 s (sans marqueur) / ≤ TTL._
- `2026-06-24` 💾 **Outillage d'image appareil (Phase 6 — rootfs RO + `.img`)** : préparation de la distribution (logiciel gratuit + kit matériel payant). Nouveau dossier `sosguide/image/` : (1) **`sosguide-harden.sh`** — minimisation/durcissement **idempotent** d'une Raspberry Pi OS Lite (purge Bluetooth/audio/mDNS/cups/rsyslog/swap/man, désactive timers `apt-daily`+radio BT, `swappiness=0`, boot sobre, LED d'activité éteinte ; **n'altère ni SSH, ni WiFi, ni `wpasupplicant`**) ; (2) **`sosguide-readonly.sh`** — rootfs lecture-seule via `overlayroot` tmpfs (résistance aux coupures), **garde-fou** refusant l'activation tant que `/var/lib/sos-guide` n'est pas un **montage persistant séparé** (anti-perte d'identité/Redb) ; (3) **`README.md`** — mode opératoire reproductible (image de base figée → install nœud → durcir → partition de données → rootfs RO → purge identité → capture `dd`+`pishrink` → BOM du kit). Recettes `just image-harden`/`image-readonly` à **cible explicite obligatoire** (jamais la prod `{{host}}`). En-tête justfile + CLAUDE.md à jour. **Vérifié** : `bash -n` OK sur les 2 scripts, `just --list` parse les recettes. _Reste = exécuter sur une SD/un Pi de build pour produire le `.img` (matériel)._
- `2026-06-23` 🛰️ **`firmware/esp32-lora` initialisé (Phase 4, vérifié à la compilation)** : le squelette du nœud satellite LoRa n'est plus vide. Cible **ESP32-C3 (RISC-V, `riscv32imc-unknown-none-elf`)** — toolchain Rust **standard** (pas d'`espup`/Xtensa). Workspace **séparé** (`[workspace]` vide → détaché de l'hôte ; exclu de `just sync`). `no_std`/`no_main` + `esp-hal` 1.1.1 (feature `unstable`) : init puce, LED de vie (GPIO8), gestionnaire de panique custom (pas d'`esp-backtrace`). `build_frame()` produit la trame d'alerte JSON compacte **au format v2.5** (`{"v":1,"nid":…,"typ":…,"ts":…,"hop":0,"msg":…}`) en `heapless::String` (sans tas), **interopérable octet-pour-octet** avec `sos_core::AlertPacket::to_frame`. `.cargo/config.toml` (cible + `-Tlinkall.x` + runner `espflash`), `README.md` (build/flash). **Vérifié sans matériel** : `cargo build --release` vert (ELF 126 Ko) ; `just check` hôte intact (workspace firmware indépendant). _Différé (matériel/toolchain réel) : Embassy (conflit de features `esp-hal-embassy 0.9.1`/`esp-hal 1.1.1` à figer), pilote SX127x SPI, boucle mesh, deep-sleep._

- `2026-06-23` 🚪 **Style & UX : splash / mode survie (dernier item Style&UX)** : le splash est conservé (arbitrage Phase 2.5 ; il affiche d'emblée les numéros d'urgence = accès vital immédiat). Ajout du **mode survie** : le bouton « Entrer » mémorise (`localStorage sos_guide_entered=1`) et, aux chargements suivants, le portail va **directement au contenu** (guide + carte de détresse en tête) au lieu de re-bloquer sur le splash ; le splash reste joignable à tout moment via le logo (`navLogo→goToSplash`). Servi 200 Pi. **Tous les items Style&UX cochés** ; restant audit = matériel/déploiement (LoRa/ESP32/rootfs RO/image .img), traductions humaines, cockpit gelé, dashboard GPS (mesh réel).

- `2026-06-22` 🔧 **Ajustements UX (retours utilisateur, 3e lot)** : (1) accueil — **carte de détresse remontée en premier** (juste après titre + texte de bienvenue ; `localInfoContainer` déplacé après `carteSection`). (2) `/admin` — bouton **thème clair/sombre** ajouté dans la barre du haut **à côté de la déconnexion** (`#themeToggle` câblé par `sos-theme.js`, le bouton flottant n'est donc plus injecté sur admin) ; les deux boutons en icônes carrées `.btn-ico` 32 px. Servi 200 Pi.

- `2026-06-22` 🔧 **Ajustements UX (retours utilisateur, 2e lot)** : (1) **animations retirées** sur tout le portail (`*{animation:none!important;transition:none!important}` dans `sos-theme.css`). (2) **zoom A−/A+ déplacé dans la barre du haut** (boutons `#a11yMinus`/`#a11yPlus` dans `.nav-right` ; `sos-theme.js` les câble si présents, sinon widget flottant pour les autres pages). (3) **bouton déconnexion `/admin` en icône seule** (🚪, texte retiré). (4) **onglet « Menu » (hamburger) retiré de la barre du bas** (désormais 4 onglets ; le tiroir s'ouvre par le hamburger du haut). Servi 200 Pi.

- `2026-06-22` 🔧 **Corrections UX (retours utilisateur)** : (1) **zoom A−/A+** masqué sur le splash (inopérant/mal placé là) et remonté au-dessus de la barre du bas en page principale (sinon caché) ; barre du bas stabilisée (`translateZ(0)`) contre le dédoublement mobile. (2) **page d'alerte** : bouton **« 🔊 Lire l'alerte »** (synthèse vocale ; lit cause+consignes dans la langue courante ; stop à la levée). (3) accueil : **texte d'aide de la carte retiré**. (4) `/admin` : bouton **déconnexion réduit + icône 🚪**. Tout servi 200 sur le Pi.

- `2026-06-22` 📱 **Style & UX : barre du bas d'accès rapide (+ correctif footer privacy)** : ajout sur l'accueil d'une **barre du bas** fixe (5 onglets : Carte/Urgences/Guide/Outils/Menu) reprise du prototype — grandes zones tactiles (≥60 px), `padding-bottom:env(safe-area-inset-bottom)` (notch, viewport-fit=cover déjà présent), fond thémé clair/sombre, affichée seulement sur la page principale (`goToMain`/`goToSplash` togglent `.show`+`body.bb-on`, qui ajoute le `padding-bottom` au contenu), masquée sous les overlays SOS (z-index 801 < 20000) ; les onglets défilent vers la section (`scrollIntoView`), « Menu » ouvre le tiroir existant. **Correctif** : le footer du tiroir était écrasé par la traduction (`drawerFooter.textContent`) → la mention/le lien de confidentialité disparaissaient au changement de langue ; footer scindé (`#drawerFooterText` traduit + bloc privacy permanent). `node`/HTML servis 200 sur le Pi. Item Style&UX coché. **Reste Style&UX : splash/mode survie** (déjà fonctionnel ; reste l'arbitrage de design).

- `2026-06-22` 🔠 **Style & UX : zoom de texte accessible (A−/A+)** : ajout au module partagé `lib/sos-theme.js` d'un réglage de **taille du texte** sur 4 niveaux (petit/normal/grand/très grand = 87,5/100/112,5/125 % du `rem` racine via `documentElement.style.fontSize`), **persistant** (`sos_guide_textsize`), appliqué **avant peinture** ; contrôle A−/A+ (avec niveau affiché et `aria-live`) **injecté sur toutes les pages**, en bas à droite (n'entre pas en conflit avec la nav ni le bouton de thème ; masqué sous les overlays SOS et à l'impression). Styles `.a11y-zoom` dans `sos-theme.css`. Cible WCAG 1.4.4. `node --check` OK, servi sur le Pi. Item Style&UX coché.

- `2026-06-22` 🎨 **Style & UX : gabarit CSS commun + thème clair/sombre unifié** : factorisation du thème dans un module partagé `web/lib/sos-theme.js` (clé localStorage commune `sos_guide_theme`, application **avant peinture** placée juste après `<body>`, câblage du toggle de nav `#themeToggle` **ou** injection d'un bouton flottant `.theme-btn` là où il manque). Les pages `index`/`install`/`admin`/`privacy` le chargent toutes ; suppression du bloc thème dupliqué d'`index` et des scripts inline d'`install`/`admin`. Résultat : **toggle de thème désormais présent sur TOUTES les pages** (avant : seulement l'accueil) et persistance unifiée. Les tokens (`sos-theme.css`) étaient déjà partagés ; aucune page ne redéfinit `:root`. `node --check` OK, assets servis 200 sur le Pi (`/lib/sos-theme.js`, pages). Barre de nav/pied volontairement propres à chaque contexte (citoyen vs opérateur). **Restant audit : matériel/déploiement, traductions humaines, cockpit gelé, 3 items Style&UX restants (zoom a11y, barres/tiroir, splash).**

- `2026-06-22` 🛡️ **Phase 5 fin + Phase 6 logicielle : MAJ OS, PRIVACY, durcissement, logs volatils, supervision** : `just os-update` (MAJ `apt` hors-bande : update + full-upgrade + autoremove + clean, sudo TTY, signale `reboot-required`). **PRIVACY.md affichée** : page `web/privacy.html` (rendu markdown léger thémée, charge `/PRIVACY.md`) + mention « 🔒 Aucune donnée personnelle collectée » et lien dans le pied du tiroir de l'accueil ; vérifié sur le Pi (`/privacy.html` + `/PRIVACY.md` → 200). **Durcissement systemd** : `ProtectSystem=strict` + `ReadWritePaths=/var/lib/sos-guide`, `PrivateTmp`, `ProtectKernelModules/Tunables`, `ProtectControlGroups`, `ProtectClock/Hostname`, `RestrictSUIDSGID/Realtime`, `LockPersonality`, `RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX AF_NETLINK` (compatible HTTP/DNS/DHCP + ip/iptables/hostapd en `live`). **Logs volatils tmpfs** : drop-in `configs/journald-sosguide.conf` (`Storage=volatile`, `RuntimeMaxUse=32M`) installé par `just install` (conformité nLPD : aucun journal persistant). **Supervision interne typée / zéro sudo web** acté : sous-systèmes en tâches `tokio` typées, aucune exécution shell depuis un handler HTTP. `just check` vert, justfile/--list OK. **Restant codable sans matériel/traduction : épuisé** (reste : matériel LoRa/ESP32, rootfs RO + image .img = déploiement OS, 22 traductions humaines, cockpit gelé, style priorité basse).

- `2026-06-22` 🧹 **Nettoyage avant suite : honnêteté UI + fin des données simulées** : retrait des **nœuds simulés** (accueil) et des LEDs figées « Non actif · Phase 3/4 ». `/api/status` expose désormais `activeNodes`/`meshPeers` (pairs **réels** entendus dans l'inbox, hors ce nœud — vide tant qu'aucun) en plus de `subsystems`. Accueil (`index.html`) : LEDs WiFi/LoRa/Tor pilotées par le mode réel (inactif=rouge/simulation=ambre/actif=vert), carte = borne + portée + pings de détresse réels uniquement. `/admin` : carte « Nœuds actifs (maillage) » dans la supervision + état réseau honnête ; wording « Phase 3/4 » purgé (sources officielles, autorités, hint WiFi, case LoRa) ; `install.html` idem. Nœud de test redéployé **sans simulation** (`SOS_*_MODE=off`) : `/api/status` → subsystems tous `off`, `activeNodes:0`. `just check` vert.

- `2026-06-22` 🔏 **Manifeste de version signé + vérification (Phase 5)** : intégrité et authenticité des mises à jour binaires, en réutilisant l'Ed25519 du projet. `sos-core::version` (pur, testé) : `VersionManifest {version, sha256, builtAt, signature}`, empreinte `sha256_hex` (vecteur FIPS « abc » vérifié), charge canonique `version|sha256|builtAt`, `matches_binary`. `sos-security::release` (testé) : `sign_detached`/`verify_detached` Ed25519 sur octets arbitraires (clé de **publication** distincte de l'identité du nœud ; PEM PKCS#8/SPKI, signature base64). `sos-cli sign-update <bin> <version> <clé-privée> [date]` (produit le manifeste signé, côté publication) et `verify-update <manifeste> <bin> <clé-publique>` (vérifie empreinte **puis** signature, exit 0/1). Recette `just verify-release` (vérifie le binaire release du Pi avant `just update`). `just check` vert (+8 tests : version + release), vérifié bout-en-bout en local avec des clés **openssl ed25519** (interop PEM OK) : signature valide → ✅ exit 0, binaire altéré → ❌ exit 1 (hash mismatch). **Reste Phase 5** : MAJ OS hors-bande (`apt`).

- `2026-06-22` 🔁 **Phase 5 — supervision journald + MAJ atomique avec rollback** : `just journal` collecte les avertissements/erreurs runtime du service (`journalctl -u sos-guide -p warning`) dans l'inbox d'`ERRORS.md` (mécanisme `_capture` partagé), pour curation par `just errors`/`error-curator` — ferme la boucle « erreurs runtime → registre ». `just update` réalise une **MAJ binaire atomique avec rollback** : sauvegarde l'actuel en `.prev`, installe le neuf via **rename atomique** (`mv -f` ; le process en cours conserve son inode), redémarre le service, vérifie la santé (`curl /api/status`, 6 essais) et **restaure `.prev` + redémarre** si la santé échoue. Recettes ajoutées au justfile (en-tête + `--list` propres). `just journal` testé (service inactif → aucun warning, inbox intacte) ; `just update` à lancer par l'utilisateur (sudo TTY). **À suivre** (Phase 5) : MAJ OS hors-bande, manifeste de version signé.

- `2026-06-22` 🛰️ **`/admin` : statut réel des sous-systèmes + panneau nœuds de confiance** : le panneau « Réseau du nœud — Tor · WiFi · LoRa » affichait des libellés **figés** « non actif · Phase 3/4 » alors que ces sous-systèmes sont implémentés et gatés. Désormais `/api/status` expose `subsystems` (modes `SOS_NET_MODE`/`SOS_RADIO_MODE`/`SOS_GW_MODE` passés au portail via `PortalConfig::subsystems`), et `/admin` rend des badges **honnêtes** : `désactivé` (gris) / `simulation` (ambre) / `actif` (vert) pour WiFi, LoRa et Tor. Ajout du **panneau « Nœuds de confiance »** (l'UI manquante de la gestion admin du registre) : liste des pairs connus, remplacement du `trusted_nodes.json` (POST) et purge (DELETE) à chaud. `just check` vert ; déployé sur `192.168.1.133:18080` (radio/gateway en `simulate`).

- `2026-06-22` 📟 **Vitaux santé exposés dans `/admin`** : `GET /api/admin/vitals` (auth) appelle `sos_cli::health::collect` sur le **pool bloquant** (`spawn_blocking`, car E/S `/proc`+`df`) ; le panneau « État du nœud — temps réel » de `/admin` affiche désormais température, mémoire utilisée %, disque utilisé % et charge 1 min en plus de l'identité/phase. `sos-portal` dépend de `sos-cli` (lib). `just check` vert ; vérifié sur le Pi (`/api/admin/vitals` → temp 61,8 °C, RAM 17 %, disque 15 % ; 401 sans auth). Déployé pour test sur `192.168.1.133:18080` (build debug, `SOS_RADIO_MODE`/`SOS_GW_MODE`=simulate, `SOS_NET_MODE`=off).

- `2026-06-22` 🩺 **`sos-cli` — sonde de santé + chien de garde (Phase 5)** : la crate `cli` (stub) devient bibliothèque + binaire `sos-cli`. `health` : vitaux du nœud (température `/sys/class/thermal`, mémoire/charge `/proc`, disque via `df -kP`) ; **analyse pure séparée de l'acquisition** (`parse_thermal`/`parse_meminfo`/`parse_loadavg`/`parse_df` + dérivés `*UsedPct`, tous testés sur échantillons réels), tout vital illisible → `null` (jamais de panique). `sos-cli health` imprime un JSON ; recette `just vitals` (remplace le shell `health`). `watchdog` : wrapper sûr de `/dev/watchdog` (`bcm2835_wdt`) — armé à l'ouverture, **caressé uniquement tant qu'une sonde applicative** (connexion TCP au démon) le juge sain, **désarmé proprement au `Drop`** (octet magique `V`, pas de reboot à l'arrêt volontaire) ; `sos-cli watchdog [device] [secs] [sonde]`. `just check` vert (+9 tests, 0 warning), build natif Pi OK ; `sos-cli health` vérifié sur le Pi (temp 59,9 °C, RAM 17 %, disque 15 %, charge). **À suivre** : unité systemd `sos-watchdog.service` au déploiement (non lancé en test pour ne pas armer le watchdog hors service) ; intégration de la sonde santé dans `/admin` et la supervision journald.

- `2026-06-22` 🧅 **`sos-gateway` — service caché Tor v3 à surface restreinte (Phase 4, code, démon différé)** : la crate `gateway` n'est plus un stub. Sur le canal Tor (3ᵉ canal avec WiFi/LoRa), le nœud n'expose qu'un **manifeste** minuscule : `manifest::build` (pur, testé) = `{service, nodeId, version, phase, alertActive}` — **jamais** le portail, l'admin, ni la config (test garde-fou `manifest_leaks_no_secret_fields`). `torrc::torrc` (pur, testé) génère le `torrc` du service caché **v3** (`HiddenServiceVersion 3`, `HiddenServicePort 80 → loopback`, `SocksPort 0`). Orchestrateur `run()` gaté `SOS_GW_MODE` (`off` défaut / `simulate` = sert le manifeste en HTTP loopback via axum, sans Tor / `live` = génère le torrc puis démarrage `tor` **différé**, démon absent du Pi). Le manifeste reflète l'état **en direct** via un `watch::Receiver<RuntimeSignal>` souscrit sur le canal du portail (aucun nouvel état partagé). Câblage app : `gateway_config` (`SOS_GW_MODE`/`SOS_GW_BIND`/`SOS_GW_HS_DIR`), spawn avec `alert_tx.subscribe()`. `just check` vert (+8 tests gateway, 0 warning), build natif Pi OK ; vérifié sur Pi (simulate) : `curl 127.0.0.1:9099` → manifeste restreint, **phase bascule STATE_PROVISIONING→STATE_EMERGENCY** après install (canal `watch`), zéro secret. **Différé** : lancement du démon `tor` (ou Arti), canal de **sortie** (relais des alertes/sources via Tor), distribution de la clé publique.

- `2026-06-22` 🔐 **Gestion admin du registre de confiance + persistance Redb** : le registre des pairs devient administrable et durable. `sos-storage` : table `trusted` (`save_trusted`/`load_trusted`/`clear_trusted`, vidée au `factory_reset`). `sos-security` : `KeyRing::clear_trusted_nodes` (réinitialise à la seule clé propre) + `trusted_node_ids` (listing trié). `sos-portal` : `GET /api/admin/trusted` (liste les IDs, auth), `POST` (remplace le registre : persiste dans Redb **puis** recharge le trousseau partagé à chaud → la radio voit les nouveaux pairs sans reboot), `DELETE` (vide, le nœud reste de confiance) ; tous sous `require_admin` + CSRF. **Bug de rotation corrigé** : `regenerate_node_key` recharge désormais le registre persisté dans le trousseau neuf (avant : registre perdu jusqu'au redémarrage). `apps/sos-guide` : boot **priorise Redb** (source de vérité administrable) puis **importe** le fichier v2.5 (et le persiste). `just check` vert (+4 tests storage/security, 0 warning), build natif Pi OK ; vérifié sur Pi : `POST` `{loaded:1}` → `GET` `[raspberrypi,voisin]` → `DELETE` 204 → `GET` `[raspberrypi]`, et persistance au reboot (« registre chargé (Redb) count=1 »). **À suivre** : panneau UI `/admin` (l'API est utilisable au curl).

- `2026-06-22` 🤝 **Registre des nœuds de confiance chargé au démarrage** : `apps/sos-guide` lit `SOS_TRUSTED_NODES` (défaut `/etc/sos-guide/trusted_nodes.json`, format v2.5 `{"nodes":{"<id>":{"public_key":"<PEM SPKI>"}}}`) et appelle `KeyRing::load_trusted_nodes` avant de partager le trousseau. **Débloque le maillage multi-nœuds** : sans registre, le nœud ne fait confiance qu'à sa propre clé et rejette (`Untrusted`) toute alerte d'un pair ; avec, les alertes signées par les pairs déclarés sont vérifiées, admises et relayées. Fichier absent/illisible = non fatal (log honnête, confiance limitée au nœud). Doc des variables d'environnement complétée dans `main.rs` (ajout `SOS_TRUSTED_NODES`/`SOS_NET_MODE`/`SOS_RADIO_MODE`/`SOS_RADIO_DEVICE`). `just check` vert, build natif Pi OK ; vérifié sur le Pi (clé pair générée via openssl ed25519 → journal « registre des nœuds de confiance chargé count=1 »). **À suivre** : gestion admin du registre (`/admin` + persistance Redb) et **préservation du registre lors d'une rotation de la clé du nœud** (actuellement `regenerate_node_key` repart d'un `KeyRing::generate` qui ne contient que la clé propre → registre perdu jusqu'au prochain démarrage).

- `2026-06-22` 📻 **`sos-radio` — transport + relais mesh LoRa (Phase 4, code, matériel différé)** : la crate `radio` n'est plus un stub. Le **codec de trame** (JSON v2.5, signature Ed25519, `unique_id`) était déjà dans `sos-core` ; `sos-radio` ajoute le **transport** + la **logique de relais**. Modules : `relay::evaluate` (pur, testé) = décode → **vérifie la signature** contre le registre de confiance (trame non signée/nœud inconnu → `Untrusted`, **jetée** : jamais affichée ni relayée) → admet (dédup + anti-rejeu via `AlertInbox`) → prépare la trame à rediffuser (`hop++` tant que `< max_hop`) ; `link` = abstraction `RadioLink` scindable (`FrameSink` clonable + `FrameSource`) avec `SimLink` en mémoire (résout le conflit d'emprunt `send`/`recv` dans `select!`) ; `device` = pilote série/SPI réel **différé** (`open` échoue proprement, aucun matériel LoRa branché) ; orchestrateur `run()` gaté `SOS_RADIO_MODE` (`off` défaut / `simulate` transport mémoire / `live` no-op journalisé). **Câblage portail↔radio** : `inbox` et `keyring` de `NodeState` passés en **`Arc` partagés** (la radio vérifie les signatures, suit les rotations de clé, et admet les alertes mesh dans la boîte affichée par le portail) ; `NodeState.radio_tx` (`mpsc`) — `POST /api/alerts` pousse la trame signée vers le maillage (`try_send` non bloquant, remplit le TODO `lib.rs`). App `main.rs` : `Arc<RwLock<KeyRing>>` + `Arc<Mutex<AlertInbox>>` partagés, canal radio (capacité 64), spawn de l'orchestrateur gaté `SOS_RADIO_MODE`. **Vérifié** : `just check` vert (+18 tests : 14 radio incl. relais hop++/dédup/anti-rejeu/untrusted, +4 wiring ; 0 warning), `just build` natif Pi OK (28 s), simulate sur Pi = `POST /api/alerts` 201 → alerte dans `lora_inbox.json` (`verified:true`) → journal radio « trame locale diffusée sur le maillage ». **Limite connue** : tant que `load_trusted_nodes` n'est pas câblé au démarrage, le nœud ne fait confiance qu'à **sa propre clé** ⇒ les trames d'autres nœuds seront `Untrusted` (chargement du registre de confiance = item suivant). **Différé** : pilotes matériels (SX1276/T-Beam), choix Reticulum, firmware ESP32.

- `2026-06-22` 📡 **Phase 3 — Réseau local `sos-network` (code complet, go-live différé)** : implémentation du réseau souverain hors-Internet, **gatée par `SOS_NET_MODE`** (`off` par défaut → aucun socket, aucune mutation système : `wlan0` étant la ligne SSH du Pi, l'AP réelle reste désactivée pour éviter le lockout). Modules créés (tous générateurs/codecs **purs et testés**) : `plan` (décision AP : provisioning→`SOS-SETUP-XXXX` ouvert · urgence sans alerte→`SOS-GUIDE` WPA2 · urgence + alerte→`SOS-GUIDE` ouvert, repli sûr sans clé), `dns` (fait main, zéro dep C : toute requête `A`→IP du nœud pour le portail captif, `AAAA` vide car IPv6 off), `dhcp` (fait main, **sans baux persistés** — pool 10.0.0.10–250 en mémoire, conformité nLPD), `hostapd` (génération conf open/WPA2 + applier gaté `live`), `firewall` (argv `iptables` : `FORWARD DROP` + hashlimit 30/s/IP + DoT/853 rejeté), `iface` (`ip addr/link` + sysctl IPv6 off). Orchestrateur `run()` (modes `off`/`simulate`/`live`) + **transition à chaud** via canal `watch<RuntimeSignal>` portail→réseau (l'AP bascule protégé↔ouvert selon l'alerte sans reboot). `WIFI_SSID` déplacé dans `sos-core` (source unique partagée portal/network). Câblage : `NodeState.alert_tx`, `set/clear_alert`/`install_node`/`admin_reset` émettent le signal ; app `main.rs` crée le canal, lit la clé WiFi persistée et `SOS_NET_MODE`, spawn l'orchestrateur. Outillage : `just net-sim` (test simulate sans risque), `just wifi-on`/`wifi-off` (différées, garde-fou `SOSGUIDE_GO_LIVE`), service systemd `Environment=SOS_NET_MODE=off`. `just check` **vert** : **+32 tests sos-network**, 0 warning. Go-live réel reporté (eth0/dongle + paquets). _Restent en Phase 3 : néant côté code ; activation matérielle déléguée au go-live._

- `2026-06-21` 🛠️ **Passage au modèle DEV-SUR-PI (build natif), abandon de la cross-compile musl** : décision utilisateur — on continue d'éditer sur le PC (dépôt git = source de vérité) mais on **synchronise** les sources vers le Pi (`rsync`) et on **compile/exécute nativement** dessus (`aarch64-unknown-linux-gnu`, glibc ; Rust 1.95 + gcc 14 déjà présents sur le Pi). `justfile` refondu : `just sync` (rsync PC→`~/sosguide-v1`), `just build`/`just pi` (`cargo build` debug/release **sur le Pi** via SSH, env `~/.cargo/env`), `just run` (lance le nœud sur le Pi, `0.0.0.0:18080`), `just install` (binaire `/usr/local/bin` + webroot `/var/www` + service systemd depuis le build natif) ; `just check` reste **local** (feedback rapide). Réglages cross-compile (`rust-toolchain.toml`, `.cargo/config.toml`) exclus du sync. Contrainte « binaire statique musl / zéro dep C » **levée** ; choix pur-Rust déjà livrés (tuiles `curl`, QR `qrcodegen`) conservés. **Validé sur le Pi** : 1er build natif debug 3 min 20 s (RAM ~0,8 Go pic, pas d'OOM), binaire 5,4 Mo, nœud démarré `STATE_PROVISIONING`, `GET /` → 200, identité Ed25519 persistée. CLAUDE.md (Plateformes & build, Workflow) + CHANGELOG à jour.

- `2026-06-21` 🗺️ **Carte de détresse unifiée avec les tuiles OSM + plein écran + cercle 30 m** : la carte de détresse de l'accueil utilise désormais les **tuiles OpenStreetMap en fond** (mosaïque `/tiles`, hors-ligne), avec une surcouche canvas : **cercle de portée WiFi ~30 m géo-référencé** (mètres/pixel = `156543·cos(lat)/2^z`), borne au centre, pings anonymes, nœuds simulés (maillage Phases 3-4). **Mode plein écran** (Fullscreen API + repli CSS). La mosaïque autonome `#localMap` est retirée (fusion demandée : « une seule carte »). `TILE_ZOOM` passe 16→**18** (~750 m de côté, ~0,39 m/px) pour que le cercle de 30 m soit net (~76 px). `ResizeObserver` gère le rendu après le splash. Vérifié en local : download 25 tuiles `z:18`, page sert `carteMap/carteFull`, `localMap` retiré. `just check` : 66 tests, 0 warning.

- `2026-06-21` 🎛️ **Dashboard admin humanitaire + login + factory reset + GPS DMS** : (1) **Page de login** stylée — l'auth Basic native est remplacée par une **session par cookie** (`sos_admin`, `HttpOnly`+`SameSite=Strict`, TTL 8 h, jeton `random_token`) ; `POST /api/admin/login` + `/logout` + sonde `GET /api/admin/session` ; `require_admin` accepte cookie **ou** Basic (curl/tests intacts) ; `/admin` sert un shell dégaté qui affiche le formulaire puis le tableau de bord. (2) **Refonte `/admin`** en dashboard sectionné (barre supérieure + pastille de phase live + héro mission + cartes), phrase d'intro retirée, **tous les IDs/handlers conservés**. (3) **Retour aux valeurs d'usine** — `Store::factory_reset` (efface config/mdp admin/alerte/officiel, garde l'identité) + `POST /api/admin/reset` (purge tuiles + sessions) → nœud en provisioning ; bouton zone danger, double confirmation. (4) **GPS DMS** — l'admin accepte désormais `48°39'14.7"N 2°20'56.1"E` (ou décimal) via un champ qui convertit en lat/lon → corrige « position GPS non renseignée ». (5) **Cohérence audit-control** : service systemd corrigé (`/admin` au lieu de `/dashboard`, typo `sosguide.fr`, `SOS_TILES_DIR` explicite), inbox `ERRORS.md` des captures résolues vidé. `just check` vert : **66 tests** (+1 `factory_reset`), 0 warning ; flux validé en local (login cookie, session, reset → provisioning, DMS→48.6541/2.3489).

- `2026-06-21` 🧭 **`/audit` fusionnée dans `/admin`** : la page publique `/audit` est retirée (fichier `audit.html`, route `/audit` et handler `audit_page` supprimés) ; son contenu (« État du nœud — temps réel » via `/api/status`, polling 5 s + « Conformité & principes ») est déplacé dans `/admin` **juste après le bloc Alerte** (section `#nodeState`, auth admin). `/api/status` est conservé (désormais consommé par l'admin). Validé en local : `/audit`→404, `/api/status`→200, `/admin` porte « État du nœud »/« Conformité ». `just check` vert : 65 tests, 0 warning.

- `2026-06-21` 🗺️ **Carte du lieu hors-ligne (tuiles OSM)** (item Phase 2 « tuiles OpenStreetMap réelles ») : stratégie tranchée avec l'utilisateur = **récupération à l'install + cache**. Contrainte décisive levée : le client est sur un **WiFi captif hors-ligne** (ne joint que le nœud) ⇒ les tuiles **doivent** être servies par le nœud ; et tout client HTTPS Rust (`ring`/asm C) **casserait** le binaire **statique aarch64-musl pur-Rust** (lié par `rust-lld`, sans toolchain C). Solution retenue : le nœud télécharge via le **`curl` système** (TLS hors du binaire) une grille `5×5` au zoom 16 (25 tuiles, ~1,3 km, User-Agent OSM honnête) autour du GPS, dans `SOS_TILES_DIR` (`/var/lib/sos-guide/tiles`), servie hors-ligne par un `ServeDir` nesté en `/tiles` (+ `meta.json`). `POST /api/admin/map` (auth admin + CSRF) ; déclenché automatiquement en fin de wizard `/install` (best-effort, non bloquant) et re-déclenchable en `/admin`. Frontend : `index.html` rend une **mosaïque de tuiles centrée sur le nœud + marqueur 📍 + attribution © OSM** (zéro lib JS, CSP `'self'` intacte), masquée si aucune tuile (repli sur le schéma). Maths slippy `lonlat_to_tile` testées. Dégradation honnête : `502` si hors-ligne / `curl` absent. Cross-compile aarch64-musl **revérifiée OK** (build pur-Rust préservé). `just check` vert : **65 tests** (+1), 0 warning. Validé en local (Genève) : install→`map {"downloaded":25,"failed":0}`, `/tiles/meta.json` 200, tuile centrale 200 `image/png`, 25 tuiles sur disque, `401` sans auth.

- `2026-06-21` 📇 **QR WiFi de l'affiche** (item « wizard QR / STARTER », Phase 2 clôturée) : le nœud génère un **QR Code de jonction WiFi** (`WIFI:S:SOS-GUIDE;T:WPA;P:<clé>;;`, caractères réservés échappés) en **SVG autonome sans prologue XML** (s'inscrit en `innerHTML` *et* se sert en `image/svg+xml`), encodé par `qrcodegen` (port pur Rust, zéro dépendance ; `encode_text` faillible géré). `/install` renvoie `wifiSsid` + `wifiQr` → le wizard affiche le QR sur le panneau de fin (scanner = rejoindre le réseau) à côté de la clé. `/admin` : `GET /api/admin/wifi-qr` (**auth admin** — le QR encode la clé secrète, masquée du public ; servi via `<img>`, Basic en cache du navigateur) affiche le QR dans le panneau réseau + bouton « Réimprimer » (ouvre le SVG seul) ; rafraîchi après régénération de la clé. SSID figé en constante `WIFI_SSID="SOS-GUIDE"`, **réutilisée par la Phase 3** (hostapd) — source unique. _Volet STARTER (langue par défaut) déjà couvert par `defaultLang`._ `just check` vert : **64 tests** (+2), 0 warning. Validé en local : install→QR sans prologue, `/api/admin/wifi-qr` 200 `image/svg+xml` (clé absente en clair), 401 sans auth, config publique masque la clé.

- `2026-06-20` 📶 **WiFi protégé en veille, ouvert en alerte** : décision produit affinée. Le mot de passe WiFi saisi à la main est retiré (`/install`, `/admin`) ; le nœud **génère** la clé à l'install (`sos-security::token::generate_wifi_key`, alphabet lisible, rejet du biais modulo) et la **renvoie au wizard** qui l'affiche pour impression sur l'affiche du lieu. `/admin` l'affiche (projection admin n'en masque plus rien : `ADMIN_HIDDEN_CONFIG_KEYS` vidé ; reste secret côté public via `SECRET_CONFIG_KEYS`) et permet de la **régénérer** (`regenerateWifiKey` → renvoie la nouvelle clé). _La bascule réelle hostapd WPA↔ouvert selon `ActiveAlert` relève de `sos-network` (Phase 3)._ Spec mise à jour (CLAUDE.md `STATE_EMERGENCY`). `just check` vert : **62 tests** (+3), 0 warning.

- `2026-06-20` 🔒 **Durcissement des entrées (Phase 2, clôturé)** : (1) **anti-XSS** — primitive `sos-security::validate_text`/`validate_secret` (refuse `<`/`>` + caractères de contrôle, borne la longueur en *caractères*) ; le portail valide récursivement tout document de config (install + admin, profondeur ≤ 16), les consignes d'alerte, les bulletins officiels et le message d'alerte publique — `wifiPassword` traité en secret (chevrons tolérés, jamais rendu). (2) **CSRF** — `origin_allowed` : sur toute API mutante (`/api/install`, `/api/admin/*`), si `Origin` est présent il doit égaler `Host` (403 sinon) ; les clients sans `Origin` (curl, tests) passent. (3) **Rate-limit auth admin** — `AuthThrottle` : tarpit croissant (1→2→4 s, plafonné `AUTH_MAX_DELAY`) après `AUTH_FAIL_THRESHOLD`=5 échecs dans une fenêtre de 60 s ; **succès = délai nul + reset**, l'admin n'est jamais verrouillé. `just check` vert : **59 tests** (+9), 0 warning. Validé en local : install XSS→422, CSRF inter-site→403, brute-force 6-8→1/2/4 s, bon mdp juste après→200 en 50 ms, rotation WiFi (clé `<…>`→200, >63 c→422, sans CSRF→403, absente de la config publique).

- `2026-06-18` 🔒 **Numéros d'urgence cohérents (pays, pas langue)** : `buildUrgences` source les numéros depuis config-du-nœud > table pays `COUNTRY_EMERGENCY` (miroir wizard), jamais depuis `langData` ; `number` retiré des `urgences` des 29 `data/*.json` (divergence par langue rendue impossible). **Corrigé** au passage : filtre pays de `/api/official` lisait `countryCode` au top-level alors qu'il est dans `establishment.countryCode` → montrait tous les pays ; `node_country_code` lit le bon chemin (nœud ML → ML+global only). Validé en local + `just check` 50 tests / 0 warning + JS 3 pages OK ; redéployé sur le Pi (8080).
- `2026-06-18` ✅ **Carte d'accueil personnalisée** : champ `venueType` (mairie/école/commerce/tabac/dispensaire/lieu public/autre) à l'install + `/admin` → icône & badge adaptés sur l'accueil ; champ `sponsor` optionnel crédité (« Borne mise à disposition par … ») = piste de **sponsoring local** (financement propre, vs lien crypto sur l'outil d'urgence : écarté). Exposés en config publique (non sensibles). Validé en local (install commerce+sponsor → config publique/admin, bascule tabac). JS des 3 pages vérifié (`node --check`).
- `2026-06-18` ✅ **Phase 2.5 — Clôture (7 items)** : (1) **fiabilité & sécurité de l'alerte** — persistance Redb ACID sans SPOF + test `active_alert_carries_no_personal_data` figeant la garantie vie privée (cause/consignes/horodatage uniquement) ; (2) **transmission aux autorités** — destinataires configurables en `/admin`, ajoutés à `SECRET_CONFIG_KEYS` (jamais publics : nouvelle projection `admin_config` vs `public_config`, test dédié), statut honnête « canal · Phase 3/4 » + compteur « en attente de relais » ; (3) **outil Morse** (tableau international + émission lumineuse plein écran + son Web Audio, hors-ligne) ; (4) **LED état réseau** honnêtes (LoRa/Tor/WiFi rouge « non actif · Phase 3/4 ») ; (5) **one-page** confirmé (splash d'entrée conservé, décision produit) ; (6) **navigation** (nav + tiroir) ; (7) **bibliothèque catégorisée** (sections pilotées par les JSON). `just check` vert : **50 tests**, 0 warning ; flux validé en local (autorités masquées au public/visibles admin, Morse+LED servis, install→config→projections). **Restent ouverts (hors auto-complétable)** : traductions vérifiées des 22 langues (relecture **humaine** obligatoire — jamais d'auto-traduction de consignes vitales) et i18n complet des pages opérateur `/install`·`/admin`·`/audit` (gros chantier de contenu, dépend des traductions). Phase 3 (réseau) débloquée pour la suite.
- `2026-06-18` ✅ **Phase 2.5 — Sources officielles & ingestion** (dernier morceau avant Phase 3) : `sos-core::official` (`OfficialBulletin` + catégorie typée `OfficialCategory`, `OfficialCache` avec dédup source+titre, tri récent-d'abord, cap 50, filtre pays), persistance Redb (`save/load/clear_official`, table `official`, survit au reboot), API portail `GET /api/official` (public, filtré sur `countryCode` du nœud + bulletins globaux, libellés de catégorie) + `POST/DELETE /api/admin/official` (auth admin, **import manuel** = repli d'acquisition hors-ligne). Frontend : bulletins affichés **sous les consignes locales** sur la page SOS (titre i18n 7 langues), panneau d'import + liste + purge en `/admin` avec statut honnête « récupération auto à la connectivité · Phase 3/4 ». Flux validé bout-en-bout en local (install FR → import FR/global/CH → `/api/official` filtre CH, trie par date → 422 cat. inconnue, 401 sans auth → purge). `just check` vert : **48 tests**, 0 warning. Reste de la Phase 2.5 : traductions vérifiées des 22 langues, index one-page, nav tiroir, bibliothèque, Morse, transmission aux autorités (canal de sortie Phase 3/4).
- `2026-06-17` ✅ **Phase 2.5 — i18n de la page SOS** : helper partagé `web/lib/sos-i18n.js` (libellés de cause par `AlertType` + consignes génériques), la bascule SOS s'affiche dans la langue du citoyen (choix explicite > `defaultLang` du nœud > navigateur), **repli français**. 7 langues vérifiées (fr/en/de/it/es/pt/nl) ; les 22 restantes (dont romanche) attendent une relecture humaine. Vérifié : helper servi (200) + logique de repli (node). _Consignes locales admin non traduites (volontaire)._
- `2026-06-17` ✅ **Phase 2.5 — Langue & pays à l'install** : 1ère étape « Langue » du wizard `/install` (grille des 29 langues → `defaultLang`) ; le **pays se déduit de la langue** (liste ordonnée, défaut auto) et **adapte l'interface** — préremplissage des numéros d'urgence selon le pays (FR/CH/… repli 112), `countryCode` persisté (numéro tapé à la main jamais écrasé). Validé en local : config publique expose `defaultLang`/`countryCode`, secrets masqués. _Items faits hors ordre strict (dépendance : pays requis avant l'ingestion des sources)._
- `2026-06-17` ✅ **Phase 2.5 — Carte de détresse** (#2) : canvas accueil (nœud central + portée + nœuds WiFi/LoRa/Tor + zoom, DPR-aware, thème), **ping de détresse anonyme** (vie privée) et **tableau des nœuds actifs** ; données simulées (maillage en Phases 3-4). `/admin` : **contacts d'urgence restylés** (cartes + icônes + `type=tel`). Audit enrichi des exigences user : alerte **sans faille / sécurisée / respect vie privée**, **transmission aux autorités** (pompiers…), **LoRa réservé aux alertes** (Phase 4), **langue + pays à l'install** (interface adaptée au pays), **toutes les pages multilingues**. Pages validées en local (`/` + carte, `/admin` auth + contacts/alerte).
- `2026-06-17` ✅ **Phase 2.5 — Mode alerte → page SOS** (tranche 1) : `sos-core::ActiveAlert` (cause `AlertType` + consignes locales ≤ 2000 c), persistance Redb (`save/load/clear_active_alert`, survit au redémarrage), `GET /api/alert` (public) + `POST/DELETE /api/admin/alert` (auth admin, `FIN_ALERTE` = clôture), **bascule SOS plein écran sobre** sur l'index (polling) + panneau « Alerte d'urgence » dans `/admin`. Flux validé de bout en bout en local (install → émission → page SOS → levée ; sans auth 503, cause inconnue 422). `just check` vert : 41 tests, 0 warning. Reste : sélection du pays à l'install + ingestion auto des sources officielles (écriront la même alerte).
- `2026-06-16` 🗑️ **Gestion des médias retirée** de `/admin` (routes `/api/admin/media`, handlers `media_*`, `safe_media_filename`, UI d'upload, CSS) — décision produit (surface d'attaque réduite). `just check` vert : 37 tests, 0 warning. Service statique du webroot inchangé.
- `2026-06-16` 🗺️ ROADMAP étendue depuis `prototype-web-sosguide/` : nouvelle **Phase 2.5** (carte de détresse interactive, ping, tableau des nœuds actifs, **mode alerte admin → page SOS plein écran**, one-page, nav tiroir, LED statut LoRa/Tor, bibliothèque catégorisée, Morse) à traiter avant la Phase 3 ; section **Style & UX** déplacée en fin d'audit (gabarit CSS, thème, zoom a11y, design barres/tiroir, splash). Aucun code produit modifié.
- `2026-06-14` ✅ Audit de correctness (tout vert : ~38 tests, 0 warning, Pi cohérent). Carte : étape **Position & carte** dans le wizard (échelle aucune/ville/pays/monde + GPS du nœud + aperçu radar SVG hors-ligne) ; config carte éditable en `/admin` ; **panneau réseau** Tor/WiFi/LoRa (config + statut « non actif Phase 3/4 », honnête car ces crates sont des squelettes). Frontend pur, persistance via la config existante. Concept de dashboard humanitaire collectif (pings GPS, pas de clients) noté pour le maillage.
- `2026-06-14` ✅ `/install` transformé en **wizard 4 étapes** (Lieu · Contacts · Message & WiFi · Accès admin), barre de progression, validation par étape, redirection vers `/admin` après installation. Pi resynchronisé après coupure : binaire + pages déployés (couleurs, médias, secrets, wizard), nœud en phase EMERGENCY, identité persistée.
- `2026-06-13` ✅ `/admin` : gestionnaire de **médias** (upload PUT brut sans dépendance multipart, liste, suppression ; `safeMedia` côté serveur, SVG exclu, cap 20 Mo) ; **rotation fine des secrets** (`/api/admin/secrets` : mot de passe admin, WiFi, régénération de la clé Ed25519 du nœud) ; **dimensions des champs** corrigées (`box-sizing`). Flux validé de bout en bout. +1 test.
- `2026-06-13` ✅ Couleurs corrigées sur `/install`, `/admin`, `/audit` (fond `var(--bg)` manquant → texte clair sur blanc ; + synchro du thème avec l'index). Améliorations UX queutées pour la prochaine session (wizard étape-par-étape, nav/CSS unifiés, index one-page sans splash, OpenStreetMap hors-ligne).
- `2026-06-13` ✅ Déploiement à jour sur le Pi (`192.168.1.133`) : binaire + webroot ; `/`, `/install`, `/audit` servis (200). Corrigé : `just install` ne redémarrait pas le service à chaud (ERR-002, `enable --now` → `restart`).
- `2026-06-13` ✅ **Phase 2 — pages portail** : `/install` (wizard → écrit config + mot de passe admin dans Redb, bascule EMERGENCY), `/admin` (auth HTTP Basic, empreinte SHA-256 itérée + sel, édition config fusionnée), `/audit` (état public lecture seule via `/api/status`). Sondes de portail captif étendues (Apple/Android/Windows/Firefox/GNOME/KDE/Kindle). Gating par cycle de vie. Flux install→admin→audit validé de bout en bout. `sos-security::password`, creds admin dans `sos-storage`. 10 nouveaux tests.
- `2026-06-13` ✅ `/data/config.json` servi depuis la **projection publique du store** (repli sur le fichier livré tant que non provisionné) ; `sos-core::Lifecycle` (PROVISIONING → EMERGENCY, transition à sens unique), phase déduite de `installed` et loggée au démarrage. Corrigé : capture auto d'erreurs (`just`) inopérante (ERR-001). 6 nouveaux tests.
- `2026-06-13` ✅ **Phase 1 — `sos-storage` (Redb)** : identité du nœud et config persistées (transactions ACID), projection publique `public_config` (sans `wifiPassword`), 3 tests. Câblé dans `apps/sos-guide` : identité PEM explicite → Redb → génération+persistance ; échec = éphémère (jamais fatal). Fin de l'identité éphémère au reboot.
- `2026-06-13` ✅ Refonte structurelle : `docs/` + `audit-control/` + `sosguide/` ; audit retiré du produit (img propre) et reconstruit en page locale `just audit` ; ROADMAP réécrite en plan ordonné de réalisation.
- `2026-06-13` ✅ Page d'accueil utilisateurs récupérée de la v3 et modernisée (29 langues, TTS, médias, outils, minuterie) — `sosguide/web/`.
- `2026-06-13` ✅ Webroot rendu optionnel (démarrage sur Pi vierge) ; service sur port 80.
